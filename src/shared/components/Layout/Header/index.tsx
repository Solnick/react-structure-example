import { StyledHeaderContainer } from './Header.styled';
import { Box } from '@mui/material';

export const Header = () => {
  return (
    <StyledHeaderContainer>
      <Box />
    </StyledHeaderContainer>
  );
};
